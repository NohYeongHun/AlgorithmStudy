#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main(void)
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    cout << "Hello, world!"
            "\n";
    3 + 4;

    cout << "This (\") is a quote, and this (\\) is a backslash. \n";

    // tab이 8칸의 띄어쓰기를 포함
    cout << "\tHello, world!";
}